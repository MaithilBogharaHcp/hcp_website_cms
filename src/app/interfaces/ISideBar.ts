export interface ISideBAr {
    menu : string[]
    sub_menu : submenu
}

interface submenu {
    arch: string[]
    urban: string[]
    legacy: string[]
    communication: string[]
    studio: string[]
    practice: string[]
    news: string[]
    career: string[]
    contact: string[]
}